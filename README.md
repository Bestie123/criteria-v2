# Система критериев ref3

## Обзор
Модульная система критериев разработки для AI-assisted разработки движка редактора.
Структура оптимизирована для работы с ИИ: 1 критерий = 1 файл, полная информация о контекстах.

## Структура файлов
```
Criteria-v2/
├── README.md                    # Эта документация
├── ref3/                        # Основная структура критериев
│   ├── _index.json             # Навигационная карта
│   ├── _master-list.json       # Сводный список всех критериев (генерируется build.js)
│   ├── taxonomy.json           # Категории и их описания
│   ├── stats.json              # Статистика по критериям (генерируется build.js)
│   ├── categories/             # Срезы по категориям (генерируется build.js)
│   │   ├── ai-criteria.json
│   │   ├── architecture.json
│   │   └── ...
│   ├── criteria/               # Файлы критериев (001.json - 114.json) - источник истины
│   │   ├── 001.json
│   │   ├── 002.json
│   │   └── ...
│   ├── details/                # Расширенные описания для сложных критериев
│   │   └── 091.json
│   └── scripts/                # Скрипты для управления
│       ├── add-criterion.js    # Добавление нового критерия
│       ├── build.js            # Валидация и генерация файлов
│       └── enrich-criteria.js  # Обновление categories_info (устаревший?)
```

**Примечание**: Файлы _master-list.json, categories/*.json и stats.json генерируются скриптом build.js и не редактируются вручную. Они обновляются автоматически при изменениях в criteria/.

## Формат критерия (criteria/NNN.json)
```json
{
  "id": "001",
  "text": "Краткое название критерия",
  "categories": ["architecture", "editor", "prompts"],
  "tags": ["ядро", "фундамент", "критическое"],
  "categories_info": [
    {
      "key": "architecture",
      "title": "Общие архитектурные принципы",
      "description": "Фундаментальные правила системы"
    }
  ],
  "contexts": {
    "architecture": {
      "subcategories": [],
      "descriptions": {
        "null": "Полное описание для основной категории"
      }
    },
    "processes": {
      "subcategories": ["Конструктивная критика"],
      "descriptions": {
        "Конструктивная критика": "Контекстное описание для subcategory"
      }
    }
  }
}
```

## Работа с критериями

### Добавление нового критерия
```bash
cd Criteria-v2/ref3
node scripts/add-criterion.js --text "Описание" --categories architecture,quality --tags docs
```

### Обновление после изменений
```bash
cd Criteria-v2/ref3
node scripts/build.js
```

### Примеры команд
- Добавление критерия для архитектуры: `node scripts/add-criterion.js --text "Модульность системы" --categories architecture --tags модульность`
- Сборка после изменений: `node scripts/build.js` (обновляет все генерируемые файлы)

## Инструкции для ИИ: Добавление и проверка новых критериев

**⚠️ ОБЯЗАТЕЛЬНО: Перед добавлением критериев прочитай [CATEGORY_GUIDE.md](ref3/CATEGORY_GUIDE.md)**

Когда пользователь предлагает новый критерий, ИИ должен следовать этим шагам для добавления, проверки и сверки:

### 1. Анализ предложения
- **Прочитай предложение пользователя**: Определи, является ли оно новым критерием или уточнением существующего.
- **Проверь на дубликаты**: Просмотри _master-list.json и categories/*.json на наличие похожих критериев. Если найдено совпадение по смыслу, предложи обновить существующий вместо создания нового.

### 2. Предложение вариантов
- **Проанализируй категории**: На основе содержимого categories/*.json предложи варианты добавления с обоснованием, описанием и примерами использования.
- **Варианты включают**: подходящие категории, теги, и если требуется - предложение новой категории или подкатегории.
- **Обоснование**: ссылки на аналогичные существующие критерии в категориях.
- **Описание**: краткое описание критерия, примеры применения.

### 3. Валидация формата
- **Категории**: Используй CATEGORY_GUIDE.md для правильного выбора категорий. Частые ошибки:
  - ❌ Документация/визуализация архитектуры → НЕ `architecture`, а `quality`
  - ❌ Диаграммы любого типа → `quality`, не `architecture`
  - ✅ Принципы построения системы → `architecture`
- **Подкатегории**: Используй только если критерий применяется по-разному в разных контекстах категории
- **Текст**: Краткое, но полное описание (не более 200 символов).
- **Теги**: Релевантные ключевые слова для поиска (например, "ID", "валидация", "производительность").

### 4. Сверка с существующими
- **Сравни с аналогичными**: Для каждой категории проверь, не пересекается ли новый критерий с существующими в criteria/*.json.
- **Контекст**: Если критерий имеет subcategories, добавь поле contexts с descriptions для каждого контекста использования.

### 5. Добавление через скрипты
- **Используй add-criterion.js**: Запусти скрипт с параметрами --text, --categories, --tags. Скрипт автоматически присвоит следующий id.
- **Пример команды**:
  ```bash
  node ref3/scripts/add-criterion.js --text "Новый критерий для валидации" --categories quality,processes --tags валидация,автоматизация
  ```
- **Добавь contexts**: После создания файла вручную добавь поле "contexts" в criteria/NNN.json, если требуется контекстная информация.

### 6. Проверка и сборка
- **Запусти build.js**: Скрипт провалидирует все файлы, обновит _master-list.json, categories/*.json и stats.json.
- **Проверь ошибки**: Если build.js выдает ошибки (неизвестная категория, дубликат id), исправь и повтори.
- **Подтверди**: После успешной сборки проверь stats.json на обновленное количество критериев.

### 7. Документирование
- **Обнови README**: Если новый критерий добавляет новую категорию или паттерн, предложи обновить этот README и описания категорий.
- **Детали**: Для сложных критериев создай файл в details/ с расширенным описанием (как 091.json).

### Важные правила для ИИ
- **Не редактируй генерируемые файлы**: _master-list.json, categories/*.json, stats.json создаются build.js.
- **Источник истины**: Только criteria/*.json редактируются вручную.
- **Уникальность**: Каждый критерий должен быть уникальным по смыслу и не дублировать существующие.
- **Консистентность**: Следуй формату существующих файлов, включая сортировку массивов.

## Навигация

### 1. Прочитай _index.json
- Узнай общее количество критериев
- Посмотри категории и их описания
- Определи релевантные разделы по tags

### 2. Найди критерии по категориям
- Используй taxonomy.json для понимания категорий
- Ищи в criteria/ файлы с нужными categories

### 3. Читай конкретные критерии
- Открой criteria/NNN.json для полного описания
- Используй поле contexts для понимания контекста

## Поля критерия

| Поле | Описание |
|------|----------|
| `id` | Уникальный идентификатор (001-114) |
| `text` | Краткое название |
| `categories` | Массив категорий |
| `tags` | Массив тегов для поиска |
| `categories_info` | Метаданные категорий |
| `contexts` | Контекстные описания по категориям и subcategories |

## Расширенные описания (details/)

Для сложных критериев, требующих детального описания, создаются файлы в директории details/. Они содержат:
- Подробные инструкции по реализации
- Примеры кода и шаблоны
- Валидационные правила
- Интеграцию с ИИ

Пример: details/091.json описывает шаблон структуры функционала с обязательными и желательными секциями.

## Категории (краткая справка)

**Подробное руководство: [CATEGORY_GUIDE.md](ref3/CATEGORY_GUIDE.md)**

**Справочник подкатегорий: [SUBCATEGORIES_REFERENCE.md](ref3/SUBCATEGORIES_REFERENCE.md)**

### Ключевые различия:

- **`architecture`** = КАК СТРОИТЬ систему (принципы)
  - ✅ Модульность, SOLID, структура ID
  - ❌ НЕ документация, НЕ визуализация

- **`quality`** = СТАНДАРТЫ и ДОКУМЕНТАЦИЯ
  - ✅ Любая документация (включая архитектурную)
  - ✅ Любая визуализация (диаграммы, графы)
  - ✅ Тесты, производительность

- **`editor`** = ФУНКЦИИ для пользователя редактора
  - 📝 **Что такое блоки:** Независимые единицы контента на странице (как в Notion, файлы в Notepad++, элементы в Word)
  - ✅ Типы блоков (Markdown, HTML, таблицы, изображения), UI, drag-and-drop, форматирование
  - ❌ НЕ общие принципы

- **`ai-criteria`** = КАК ИИ работает с кодом
  - ✅ Генерация, рефакторинг, декомпозиция

- **`prompts`** = ШАБЛОНЫ запросов для ИИ
  - ✅ Готовые шаблоны промптов

- **`processes`** = КАК КОМАНДА работает
  - ✅ Методологии, практики, ревью

- **`tools`** = ИНСТРУМЕНТЫ для разработки с ИИ
  - ✅ Puppeteer, CDP, автоматизация

### Подкатегории (contexts)

Используй подкатегории когда критерий применяется по-разному в разных контекстах одной категории.

**Все существующие подкатегории:**

- **quality** (9): `Архитектурная целостность`, `Рефакторинг`, `Документация`, `Best Practices & Code Review`, `Критические компоненты (ядро и несущие стены)`, `Тестирование`, `Производительность`, `Безопасность`, `UI компоненты`

- **processes** (5): `Конструктивная критика`, `Рефакторинг`, `Работа с задачами`, `Troubleshooting и отладка`, `Версионирование и история изменений`

- **prompts** (3): `Шаблоны запросов`, `Конструктивная критика (шаблоны)`, `Документация промптов`

- **editor** (5): `Форматирование текста`, `Таблицы`, `Визуализация данных`, `Блоки контента`, `Навигация и связи`

- **architecture, ai-criteria, tools**: Подкатегории обычно не используются

## Оптимизация для ИИ

- **Размер файлов**: Каждый критерий в отдельном файле (~1-2K токенов)
- **Полная информация**: Все описания и контексты в одном месте
- **Быстрый поиск**: По ID, categories, tags
- **Контекст**: subcategories сохраняют нюансы использования

## Миграция из ref2

- ref2 содержал иерархическую структуру с subcategories
- ref3 преобразует это в плоскую структуру с полным контекстом
- Все описания сохранены, включая разные формулировки для разных контекстов

## Быстрый старт

### Для ИИ-ассистентов:
1. **ОБЯЗАТЕЛЬНО**: Прочитай [CATEGORY_GUIDE.md](ref3/CATEGORY_GUIDE.md)
2. Прочитай этот README
3. Посмотри taxonomy.json для понимания категорий
4. Используй _master-list.json для проверки на дубликаты

### Для разработчиков:
1. Прочитай этот README
2. Посмотри _index.json для навигации
3. stats.json для статистики по категориям
4. categories/ для фильтрации критериев по категориям
5. Найди нужные критерии в criteria/
